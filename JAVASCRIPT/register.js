document.querySelector('.register-button a').addEventListener('click', function(event) {
    event.preventDefault();

    // Redirect to the register page
    window.location.href = "Registration.html";
});
